// path: The module provides access for working with file and directory paths. Example of the module is to join and resolve file paths or to get the directory name of a file path.

const path = require('path');
// Joining two path segments
console.log(path.join('/food', 'drink')); // Outputs: /foo/bar
